version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies..."
  build:
    commands:
      - echo "Building project..."
      - ls -la

artifacts:
  files:
    - '**/*'
  base-directory: .
